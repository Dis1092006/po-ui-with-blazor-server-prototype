@page "/purchase-order/{id}"

<PurchaseOrderView PoNumber="@PoNumber"/>

@code {
    private int PoNumber { get; set; }

    [Parameter]
    public string Id
    {
        get => PoNumber.ToString();
        set => PoNumber = int.Parse(value);
    }
}